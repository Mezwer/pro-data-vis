import { fields, averages } from "@/constants/fields";

export const packData = (data) => {
  const finalData = {};
  for (const entry of data) {
    for (const [key, value] in entry) {
      
    }
  }
}